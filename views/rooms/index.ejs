<% layout('layouts/boilerplate') %> 
    <h1>Available</h1>
    <a href="rooms/new">Add a new location</a>
    <ul>
       <% for (let room of rooms) {  %>  
        <li><a href="/rooms/<%= room._id %>"><%= room.title %> </a></li>
        <% } %> 
    </ul>
